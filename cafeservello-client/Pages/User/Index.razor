@page "/user"

<div class="search">
    <i class="bi bi-search"></i>
    <input class=""
        placeholder="Pesquisar"

    />
</div>
<style>
    .search{
        display: flex;
        padding: 1rem 1rem 1rem 1.2rem;
        align-items: center;
        background: var(--bg);
        border-radius: 0.5em;
        gap: 1em;
    }

    .search i{
        color: var(--text)
    }

    input{
        display: flex;
        width: 100%;
        height: 100%;
        border: none;
        background: none;
        color: none;
        outline: none;
    }
    
    input::placeholder{
        color: var(--text)
    }
</style>